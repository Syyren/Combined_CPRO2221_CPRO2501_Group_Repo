import React from 'react';

const Score = ({ score }) => {
    return (
        <div style={{ textAlign: 'center' }}>
            <h2>Score: {score}</h2>
        </div>
    );
};

export default Score;
